<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:Vestigium.Controls.UnderConstruction">
  <Style TargetType="{x:Type local:VestigiumUnderConstruction}">
    <Setter Property="Background" Value="#EB0F2744"/>
    <Setter Property="Foreground" Value="#E2E8F0"/>
    <Setter Property="HorizontalAlignment" Value="Stretch"/>
    <Setter Property="VerticalAlignment" Value="Stretch"/>
    <Setter Property="SnapsToDevicePixels" Value="True"/>
    <Setter Property="Focusable" Value="False"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type local:VestigiumUnderConstruction}">
          <Border x:Name="Root"
                  Background="{TemplateBinding Background}"
                  SnapsToDevicePixels="True">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="16">
              <Viewbox Width="64" Height="64" MaxWidth="96" MaxHeight="96">
                <Path x:Name="PART_Icon"
                      Data="{TemplateBinding IconData}"
                      Fill="#E8A317"
                      Stretch="Uniform"/>
              </Viewbox>
              <TextBlock x:Name="PART_Header"
                         Margin="0,16,0,0"
                         FontSize="22"
                         FontWeight="SemiBold"
                         TextAlignment="Center"
                         TextWrapping="Wrap"
                         MaxWidth="420"
                         Foreground="{TemplateBinding Foreground}"
                         Text="{TemplateBinding Header}"/>
              <TextBlock x:Name="PART_Message"
                         Margin="0,8,0,0"
                         FontSize="13"
                         TextAlignment="Center"
                         TextWrapping="Wrap"
                         MaxWidth="420"
                         Foreground="#94A3B8"
                         Text="{TemplateBinding Message}"/>
              <Button x:Name="PART_Action"
                      Margin="0,20,0,0"
                      MinHeight="32"
                      MinWidth="120"
                      Padding="16,6"
                      HorizontalAlignment="Center"
                      Content="{TemplateBinding ActionText}"
                      Command="{TemplateBinding Command}"
                      CommandParameter="{TemplateBinding CommandParameter}"
                      Visibility="Collapsed"
                      Focusable="True"
                      Foreground="#E2E8F0"
                      Background="#132744"
                      BorderBrush="#1E3A5F"
                      BorderThickness="1">
                <Button.Template>
                  <ControlTemplate TargetType="Button">
                    <Border x:Name="bd"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}"
                            CornerRadius="2">
                      <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                      <Trigger Property="IsMouseOver" Value="True">
                        <Setter TargetName="bd" Property="Background" Value="#4A90C8"/>
                        <Setter Property="Foreground" Value="#0B1524"/>
                      </Trigger>
                      <Trigger Property="IsPressed" Value="True">
                        <Setter TargetName="bd" Property="Background" Value="#E8A317"/>
                        <Setter Property="Foreground" Value="#0B1524"/>
                      </Trigger>
                    </ControlTemplate.Triggers>
                  </ControlTemplate>
                </Button.Template>
              </Button>
            </StackPanel>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsActive" Value="False">
              <Setter Property="Visibility" Value="Collapsed"/>
              <Setter Property="IsHitTestVisible" Value="False"/>
            </Trigger>
            <DataTrigger Binding="{Binding Header, RelativeSource={RelativeSource TemplatedParent}}" Value="">
              <Setter TargetName="PART_Header" Property="Visibility" Value="Collapsed"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding Message, RelativeSource={RelativeSource TemplatedParent}}" Value="">
              <Setter TargetName="PART_Message" Property="Visibility" Value="Collapsed"/>
            </DataTrigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
</ResourceDictionary>
