<UserControl x:Class="Vestigium.Controls.UnderConstruction.UnderConstructionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Grid>
    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
      <TextBlock FontSize="22" FontWeight="SemiBold" HorizontalAlignment="Center"
                 Foreground="#E2E8F0"
                 Text="{Binding Title, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
      <TextBlock Margin="0,8,0,0" Opacity="0.7" TextWrapping="Wrap" TextAlignment="Center"
                 MaxWidth="480"
                 Foreground="#94A3B8"
                 Text="{Binding Detail, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
    </StackPanel>
  </Grid>
</UserControl>
